<h1>Song: <%= @song.name %></h1>
<hr>
Artist:<%= (link_to @song.artist.name, artist_path(@song.artist)) if @song.artist %> <br>
Genre:<%= (link_to @song.genre.name, genre_path(@song.genre)) if @song.genre %>